# Production-specific overrides

# Cloudflare Web Analytics is enabled via zone-based auto-injection in
# the Cloudflare dashboard (Web Analytics > pertsfoundry.com). No code
# configuration is required. EU visitor data is excluded.
